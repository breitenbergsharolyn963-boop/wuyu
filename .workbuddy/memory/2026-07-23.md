# 2026-07-23

## 个人简历主题博客站点（Astro 5）
- 在 `E:/AI/Tasks/WorkBuddy_Task` 用 Astro 5 + 手写 CSS 从零搭建了「个人简历」主题博客。
- 功能：首页文章列表（按 pubDate 倒序）、文章详情（Markdown + Shiki 双主题代码高亮 + 标签 + 上下篇导航）、标签聚合页 `/tags` 与单标签页 `/tags/[tag]`、关于/简历页 `/about`（消费 `src/data/profile.ts`）。
- 设计：CSS 变量设计令牌、响应式 + 移动端汉堡菜单、跟随系统的明暗主题。
- 内容：`src/content/posts/` 下 4 篇示例文章；`src/data/profile.ts` 一份示例简历。

## 环境注意（重要）
- 沙箱的 safe-delete 守卫会拦截批量删除（>50 文件）。`astro dev` 启动时 Vite 要清 `node_modules/.vite/deps` 缓存 → 被拦截导致 dev 起不来。
- 规避办法：用 `npm run build` + `npm run preview`（serve 静态 dist/）做本地预览，不触发删除。dev 模式如确需，可用 `dangerouslyDisableSandbox` 关闭沙箱。
- 托管 Node：`C:\Users\lenmou\.workbuddy\binaries\node\versions\22.22.2`，npm 安装/构建均用该 PATH。

## 用本地研究内容填充站点（第二轮）
- 用户要求用本地研究内容填充简历+文章。只读扫描个人目录（Documents/Desktop/Downloads）找到研究文件，确认后读取。
- 来源文件：`Desktop/AVA/论文2.0.docx`、`论文.docx`、`摘录.docx`；`Downloads/个人事迹介绍模板（获奖学生填写）.docx`；`Desktop/AVA/script_auto_sorted.py`、`script_export_eulerian_information.py`（真实 Rocky DEM 后处理脚本）。
- 提取方法：托管 Python 隔离 venv（`C:\Users\lenmou\.workbuddy\binaries\python\envs\default`）+ `python-docx`；脚本 `extract_docx.py` 抽正文/表格，并额外抽 core_properties 与页眉页脚（作者名/单位常在标题页，段落提取拿不到）。注意 Windows 路径要用 `C:/...` 而非 `/c/...`。
- 提取到的真实信息：作者=吴宇，化学工程与制药学院 2024 级硕士，导师周燕代子副教授，三等学业奖学金；研究方向=超细粉体（FCC, 30μm）转鼓流动 DEM 仿真（Ansys Rocky）、粗粒化 CGP、RS-HDMR 敏感性分析（SALib）。
- 已替换：删除 4 篇占位文章，新建 4 篇研究文章（ultrafine-powder-dem / dem-contact-models / rs-hdmr-sensitivity / cgp-data-pipeline，含真实 Python 代码）；`profile.ts` 填真实简历；关于页「工作经历」改为「科研经历」；头像改为「吴」。
- 仍占位待用户确认：邮箱、GitHub、本科教育（原文未提供）。已在代码中以「（占位，请替换）」标注。
- 用户决定：先以占位生成，资料后续自行补充，无需我臆造。
- 首页措辞调整：用户要求首页不称「个人文章」，改为「阶段性研究」。改 index.astro 的 hero 标题/副文案/列表 aria-label/空列表提示。
